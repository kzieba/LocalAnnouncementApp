{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Announcement edit</h1>

    {{ form(edit_form) }}

    <a href="{{ path('photo_new', { 'id':entity.id }) }}">add new foto</a>

        <ul class="record_actions">
    <li>
        <a href="{{ path('announcement') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
